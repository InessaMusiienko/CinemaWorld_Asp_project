@model IEnumerable<AllFilmViewModel>
@{
    ViewBag.Title = "Favourites";
}
<link href="~/css/mine.css" rel="stylesheet" type="text/css">

<h2 class="text-center">@ViewBag.Title</h2>
<hr />

<div class="general-div">
    <section id="moviesSection">
        @if(!Model.Any())
        {
            <h2 class="text-center">You don't have any films yet!</h2>
        }
        @foreach (var film in Model)
        {
            <div class="sub-div">
                <article>
                    <img class="movie__img2"
                         src= @film.ImageUrl>
                    <div class="card-body">
                        @if(film.Genre == "Thriller")
                        {
                            <form class="input-group-sm" asp-controller="Film" asp-action="FilmByGenre" asp-route-id="@film.Id">
                                <input type="submit" value="Thriller" class="movie__tag--thriller" />
                                @if (User.Identity.IsAuthenticated)
                                {
                                    <a asp-controller="Film" asp-action="RemoveFromFavourites" asp-route-id="@film.Id" class="h_container"><i id="heart" class="far fa-heart"></i></a>
                                }
                            </form>
                        }
                        @if(film.Genre == "Fantasy")
                        {
                             <form class="input-group-sm" asp-controller="Film" asp-action="FilmByGenre" asp-route-id="@film.Id">
                                <input type="submit" value="Fantasy" class="movie__tag--fantasy" />
                                @if (User.Identity.IsAuthenticated)
                                {
                                    <a asp-controller="Film" asp-action="RemoveFromFavourites" asp-route-id="@film.Id" class="h_container"><i id="heart" class="far fa-heart"></i></a>
                                }
                            </form>
                        }
                        @if(film.Genre == "Animation")
                        {
                             <form class="input-group-sm" asp-controller="Film" asp-action="FilmByGenre" asp-route-id="@film.Id">
                                <input type="submit" value="Animation" class="movie__tag--animation" />
                                @if (User.Identity.IsAuthenticated)
                                {
                                    <a asp-controller="Film" asp-action="RemoveFromFavourites" asp-route-id="@film.Id" class="h_container"><i id="heart" class="far fa-heart"></i></a>
                                }
                            </form>
                        }
                        @if(film.Genre == "Comedy")
                        {
                             <form class="input-group-sm" asp-controller="Film" asp-action="FilmByGenre" asp-route-id="@film.Id">
                                <input type="submit" value="Comedy" class="movie__tag--comedy" />
                                @if (User.Identity.IsAuthenticated)
                                {
                                    <a asp-controller="Film" asp-action="RemoveFromFavourites" asp-route-id="@film.Id" class="h_container"><i id="heart" class="far fa-heart"></i></a>
                                }
                            </form>
                        }   
                        @if(film.Genre == "Action")
                        {
                             <form class="input-group-sm" asp-controller="Film" asp-action="FilmByGenre" asp-route-id="@film.Id">
                                <input type="submit" value="Action" class="movie__tag--action" />                                
                                <a asp-controller="Film" asp-action="RemoveFromFavourites" asp-route-id="@film.Id" class="h_container"><i id="heart" class="far fa-heart"></i></a>
                            </form>
                        } 
                        <h5>@film.Name</h5>
                        <p>@film.Description</p>

                        <a asp-controller="Film" asp-action="RemoveFromFavourites" asp-route-id="@film.Id" class="h_container"><i id="heart" class="far fa-heart"></i></a>
                    </div>
                </article>

                @if (film.Genre == "Thriller")
                    {
                        <form class="input-group-sm" asp-controller="Home" asp-action="GetDetails" asp-route-id="@film.Id">
                            <input type="submit" value="Details" class="addToFavor--thriller" />
                        </form>
                    }
                    @if (film.Genre == "Fantasy")
                    {
                        <form class="input-group-sm" asp-controller="Home" asp-action="GetDetails" asp-route-id="@film.Id">
                            <input type="submit" value="Details" class="addToFavor--fantasy" />
                        </form>
                    }
                    @if (film.Genre == "Animation")
                    {
                        <form class="input-group-sm" asp-controller="Home" asp-action="GetDetails" asp-route-id="@film.Id">
                            <input type="submit" value="Details" class="addToFavor--animation" />
                        </form>
                    }
                    @if (film.Genre == "Comedy")
                    {
                        <form class="input-group-sm" asp-controller="Home" asp-action="GetDetails" asp-route-id="@film.Id">
                            <input type="submit" value="Details" class="addToFavor--comedy" />
                        </form>
                    }   
                    @if (film.Genre == "Action")
                    {
                        <form class="input-group-sm" asp-controller="Home" asp-action="GetDetails" asp-route-id="@film.Id">
                            <input type="submit" value="Details" class="addToFavor--action" />
                        </form>
                    }            
                    @if (User.Identity.IsAuthenticated)
                    {
                        <a asp-controller="Film" asp-action="Edit" asp-route-id=@film.Id class="btn btn-light">Edit</a>
                        <a asp-controller="Film" asp-action="Delete" asp-route-id=@film.Id class="btn btn-light">Delete</a>                     
                    }

                 
                @if (User.Identity.IsAuthenticated)
                {
                   
                        @*@if(film.Genre == "Thriller")
                        {
                            <form class="input-group-sm" asp-controller="Film" asp-action="RemoveFromFavourites" asp-route-id="@film.Id">
                                <input type="submit" value="Remove from Favourite" class="addToFavor--thriller" />
                            </form>
                        }
                        @if(film.Genre == "Fantasy")
                        {
                           <form class="input-group-sm" asp-controller="Film" asp-action="RemoveFromFavourites" asp-route-id="@film.Id">
                                <input type="submit" value="Remove from Favourite" class="addToFavor--fantasy" />
                            </form>
                        }
                        @if(film.Genre == "Animation")
                        {                            
                            <form class="input-group-sm" asp-controller="Film" asp-action="RemoveFromFavourites" asp-route-id="@film.Id">
                                <input type="submit" value="Remove from Favourite" class="addToFavor--animation" />
                            </form>
                        }
                        @if(film.Genre == "Comedy")
                        {
                            <form class="input-group-sm" asp-controller="Film" asp-action="RemoveFromFavourites" asp-route-id="@film.Id">
                                <input type="submit" value="Remove from Favourite" class="addToFavor--comedy" />
                            </form>
                        }   
                        @if(film.Genre == "Action")
                        {
                            <form class="input-group-sm" asp-controller="Film" asp-action="RemoveFromFavourites" asp-route-id="@film.Id">
                                <input type="submit" value="Remove from Favourite" class="addToFavor--action" />
                            </form>
                        }   *@
                    
                }
            </div>
        }
    </section>
</div>
@*<div class="general-div">
    <section id="moviesSection">
        @foreach (var film in Model)
        {
            <div class="sub-div">
                <article>
                    <img class="movie__img2"
                        src="@film.ImageUrl" alt="Film Image">
                    <div class="card-body">

                        <h5 class="card-title mt-1">@film.Name</h5>
                        <p class="mb-0">Author: @film.Country, @film.Year</p>
                        <p class="mb-0">Description: @film.</p>
                        <p>Category: @film.Genre</p>
                    </div>
                </article>

                <form class="input-group-sm" asp-controller="Film" asp-action="RemoveFromFavourites" asp-route-id="@film.Id">
                    <input type="submit" value="Remove from Favourites" class="fs-6 btn btn-success mb-3 p-2" />
                </form>
            </div>
        }
    }
    </section>
</div>*@

